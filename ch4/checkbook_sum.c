#include <stdio.h>
#include "getline_.h"

#define MAXLINE 100

int getline_(char s[], int m);
int main(void)
{
  double sum, atof(char[]);
  char line[MAXLINE];
  int getline_(char s[], int m);

  sum = 0;
  while (getline_(line, MAXLINE) > 0)
    printf("\t%g\n", sum += atof(line));
  return 0;
}
